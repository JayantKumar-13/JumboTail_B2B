package com.jayant.JTail.enums;

// Enum representing delivery speed options for orders, used to determine shipping times and costs. STANDARD is the default option with regular delivery times, while EXPRESS offers faster delivery at a higher cost. This enum is used in the Order entity to specify the chosen delivery speed for each order.
public enum DeliverySpeed {
    STANDARD,
    EXPRESS
}
